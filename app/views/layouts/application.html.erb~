<!DOCTYPE html>
<html>
<head>
	   <title>Yasuo</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>
</head>
<body>
	  <nav class="navbar navbar-inverse navbar-fixed-top">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to 'Yasuo', root_path, class: :'navbar-brand' %>
		</div>
		<div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
				<li class="<%= active_class_for('pages') %>">
					<%= link_to 'My Page', root_path %>
				</li>
				<li class="<%= active_class_for('series') %>">
					<%= link_to 'Comic List', series_index_path %>
				</li>
				<% if user_signed_in? %>
				<li>
					<%= link_to "Log out", destroy_user_session_path, method: :delete %>
				</li>
				<% else %>
				<li>
					<%= link_to "Sign in", new_user_registration_path %>
				</li>
				<li>
				  <blockquote class="blockquote-reverse">
                
					<%= link_to "Log in", new_user_session_path %>
				  </blockquote>
				</li>
				<% end %>
			</li>
		</ul>
	</div><!--/.nav-collapse -->
</nav>
  <div class="container">
	<% if notice %>
	<p class="alert alert-info" role="alert"><%= notice %></p>
	<% elsif alert %>
	<p class="alert alert-danger" role="alert"><%= alert %></p>
	<% end %>
	    <%= yield %> <!-- ※この行は各ページを表示するための、最初から存在する。 -->
  </div><!-- /.container -->
</body>
</html>
